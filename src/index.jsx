import React from 'react';
import ReactDOM from 'react-dom/client';
import DeliveryCalculator from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <DeliveryCalculator />
  </React.StrictMode>
);
